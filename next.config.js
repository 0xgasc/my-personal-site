/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['www.augmentir.com', 'scontent-iad4-1.choicecdn.com', 'i2.seadn.io', 'opensea.io'],
  },
}

module.exports = nextConfig
